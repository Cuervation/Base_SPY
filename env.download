# BACKEND_URL is server-only — never exposed to the browser via NEXT_PUBLIC_
BACKEND_URL=http://localhost:8080/api
# NEXT_PUBLIC_API_URL kept for backwards compatibility during migration (can be removed once all references are gone)
# NEXT_PUBLIC_API_URL=http://localhost:8080/api
NEXT_PUBLIC_LOGOUT_LINK=https://apps.telefonica.com.ar/DESARROLLO-Login/Auth/SignOut?returnUrl=http://localhost:45102/login